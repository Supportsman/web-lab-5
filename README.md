# Лабораторная работа №5 – Работа с базой данных MySQL через PHP и Docker

 Погирейчик Артём  
 3 курс, МОиАИС РИСКУ  
**Вариант:** 10 (Регистрация в спортзале)

## 🎯 Цель работы
Научиться работать с базой данных MySQL через PHP, создавать таблицы, сохранять данные из формы в БД, выводить данные на странице, использовать PHP-классы для работы с таблицей и работать с Docker-контейнерами (nginx, PHP-FPM, MySQL, Adminer).

## 📋 Описание моего варианта (№10)
Система регистрации в спортзале со следующими полями:
- **Имя** (текстовое поле)
- **Дата рождения** (поле даты)
- **Тариф** (выпадающий список: Разовый/Месячный/Годовой/Студенческий)
- **Персональный тренер** (чекбокс)
- **Время посещения** (радио-кнопки: Утро/День/Вечер)

## 🏗️ Структура проекта
```
web-lab-5/
├── docker-compose.yml      # Конфигурация Docker-сервисов
├── Dockerfile              # Образ PHP-FPM с расширениями
├── nginx.conf              # Конфигурация Nginx
├── db_schema.sql           # SQL-скрипт создания таблицы
├── .gitignore              # Исключаемые файлы Git
└── www/                    # Исходный код приложения
    ├── index.php           # Главная страница с таблицей записей
    ├── form.html           # HTML-форма регистрации
    ├── process.php         # Обработчик формы
    ├── delete.php          # Скрипт удаления записей
    ├── db.php              # Подключение к БД
    └── GymRegistration.php # Класс для работы с таблицей
```

## 🐳 Docker-окружение
Проект использует 4 контейнера, связанных через Docker Compose:
- **lab5_nginx** (nginx:alpine, порт 8080) - веб-сервер
- **lab5_php** (php:8.2-fpm с pdo_mysql) - обработчик PHP
- **lab5_db** (mysql:8.0, порт 3307) - сервер базы данных
- **lab5_adminer** (adminer, порт 8081) - веб-интерфейс для управления БД

## 🗃️ База данных
**Созданная таблица:** `gym_registrations`
```sql
CREATE TABLE gym_registrations (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    birth_date DATE NOT NULL,
    tariff VARCHAR(50) NOT NULL,
    personal_trainer TINYINT(1) DEFAULT 0,
    visit_time VARCHAR(50) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**Данные для подключения:**
- Сервер: `db`
- База данных: `gym_db`
- Пользователь: `gym_user`
- Пароль: `gym_pass`

## 💻 PHP-реализация
1. **www/db.php** - подключение к БД через PDO с обработкой ошибок
2. **www/GymRegistration.php** - класс с методами `add()`, `getAll()`, `delete()` для CRUD-операций
3. **www/form.html** - HTML-форма регистрации с валидацией
4. **www/process.php** - обработчик формы с экранированием данных
5. **www/index.php** - вывод всех записей в таблице с функцией удаления
6. **www/delete.php** - скрипт удаления записей с проверкой параметров

## 🚀 Инструкция по запуску
1. Клонировать репозиторий:
   ```bash
   git clone https://github.com/ваш-логин/web-lab-5.git
   cd web-lab-5
   ```

2. Запустить Docker-контейнеры:
   ```bash
   docker-compose up -d
   ```

3. Приложение будет доступно по адресам:
   - Форма регистрации: http://localhost:8080/form.html
   - Список зарегистрированных клиентов: http://localhost:8080/index.php
   - Adminer (управление БД): http://localhost:8081

4. Для остановки контейнеров:
   ```bash
   docker-compose down
   ```

4. **Работа с PowerShell на Windows** - использование `New-Item` вместо `touch`
✅ Штрафное задание (дополнительно реализовано)


